<x-layouts::app :title="'Resultado · '.$attempt->activity->titulo"><div class="p-6"><div class="mx-auto max-w-4xl space-y-6">
    @if (session('success'))<div class="rounded-lg border border-green-300 bg-green-100 p-4 text-green-800">{{ session('success') }}</div>@endif
    <div><p class="text-sm font-semibold text-cyan-600">Intento {{ $attempt->attempt_number }}</p><h1 class="text-2xl font-bold dark:text-white">{{ $attempt->activity->titulo }}</h1></div>
    @if ($showResult)<div class="grid gap-4 sm:grid-cols-3"><div class="rounded-xl bg-cyan-600 p-6 text-white"><p class="text-sm text-cyan-100">Puntuación</p><p class="text-3xl font-bold">{{ $attempt->score }} / {{ $attempt->max_score }}</p></div><div class="rounded-xl border p-6 dark:border-zinc-700"><p class="text-sm text-zinc-500">Aciertos</p><p class="text-3xl font-bold dark:text-white">{{ $correctCount }} / {{ $totalItems }}</p></div><div class="rounded-xl border p-6 dark:border-zinc-700"><p class="text-sm text-zinc-500">Mejor puntuación</p><p class="text-3xl font-bold dark:text-white">{{ $bestScore }}</p></div></div>@else<div class="rounded-xl border border-cyan-200 bg-cyan-50 p-6 text-cyan-900 dark:border-cyan-900 dark:bg-cyan-950/30 dark:text-cyan-100">Tu intento fue recibido.</div>@endif
    <div class="flex flex-wrap gap-3"><a href="{{ route('activities.hay-ahi-ay.play', $attempt->activity) }}" class="rounded-lg bg-cyan-600 px-5 py-3 font-semibold text-white">Ver mis intentos</a><a href="{{ route('activities.show', $attempt->activity) }}" class="rounded-lg border px-5 py-3 font-semibold dark:border-zinc-700 dark:text-white">Volver</a></div>
</div></div></x-layouts::app>

<x-layouts::app :title="'Resultado · '.$attempt->activity->titulo">
    <div class="p-6"><div class="mx-auto max-w-4xl space-y-6">
        @if (session('success'))<div class="rounded-lg border border-green-300 bg-green-100 p-4 text-green-800">{{ session('success') }}</div>@endif
        <div><p class="text-sm font-semibold text-emerald-600">Intento {{ $attempt->attempt_number }}</p><h1 class="text-2xl font-bold dark:text-white">{{ $attempt->activity->titulo }}</h1></div>
        @if ($showResult)<div class="grid gap-4 sm:grid-cols-3"><div class="rounded-xl bg-emerald-600 p-6 text-white"><p class="text-sm text-emerald-100">Puntuación</p><p class="text-3xl font-bold">{{ $attempt->score }} / {{ $attempt->max_score }}</p></div><div class="rounded-xl border p-6 dark:border-zinc-700"><p class="text-sm text-zinc-500">Aciertos</p><p class="text-3xl font-bold dark:text-white">{{ $correctCount }} / {{ $totalValues }}</p></div><div class="rounded-xl border p-6 dark:border-zinc-700"><p class="text-sm text-zinc-500">Mejor puntuación</p><p class="text-3xl font-bold dark:text-white">{{ $bestScore }}</p></div></div>@else<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-6 text-emerald-900 dark:border-emerald-900 dark:bg-emerald-950/30 dark:text-emerald-100">Tu intento fue recibido. El resultado inmediato está desactivado.</div>@endif
        <div class="flex flex-wrap gap-3"><a href="{{ route('activities.numeric-sequence.play', $attempt->activity) }}" class="rounded-lg bg-emerald-600 px-5 py-3 font-semibold text-white">Ver mis intentos</a><a href="{{ route('activities.show', $attempt->activity) }}" class="rounded-lg border px-5 py-3 font-semibold dark:border-zinc-700 dark:text-white">Volver a la actividad</a></div>
    </div></div>
</x-layouts::app>
